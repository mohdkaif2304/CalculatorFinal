<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CalculatorFinal" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/my_light_primary</item>
        <!-- Keep the primary variant color for other purposes -->
        <item name="colorPrimaryVariant">@color/my_light_primary</item>
    </style>

    <!-- Define a separate style for your buttons -->
    <style name="ButtonStyle" parent="Widget.Material3.Button">
        <!-- Set the background color for your buttons -->
        <item name="android:background">@color/btn_bg_2</item>
        <!-- You can also customize other attributes for your buttons here -->
    </style>

    <style name="Theme.CalculatorFinal" parent="Base.Theme.CalculatorFinal" />
</resources>
